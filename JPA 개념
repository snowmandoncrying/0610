JPA란?
  Java Persistence API로 스프링부트는 JPA를 사용하여 데이터베이스를 관리한다
    ORM 기술 표준으로 사용한다.
    인터페이스 모음으로 이 인터페이스를 구현한 실제 클래스가 필요하다.
      실제 클래스에는 대표적으로 하이버네이트(Hibernate)가 있다.
        하이버네이트는 JPA의 인터페이스를 구현한 실제 클래스이자 자바의 ORM 프레임워크로
        스프링 부트에서 데이터베이스를 관리하기 쉽게 도와준다.
